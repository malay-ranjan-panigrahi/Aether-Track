apiVersion: v1
kind: Service
metadata:
  name: {{ include "aether-app.fullname" . }}
  labels:
    {{- include "aether-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "aether-app.selectorLabels" . | nindent 4 }}
